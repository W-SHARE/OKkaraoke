<!DOCTYPE html>
<html>
  <head>
    <title>Okkaraoke</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>
    <script src="https://unpkg.com/leaflet@1.4.0/dist/leaflet.js"></script>
    <link rel="stylesheet" href="https://unpkg.com/leaflet@1.3.1/dist/leaflet.css">

    <%= favicon_link_tag 'favicon.ico' %>
    <%= favicon_link_tag 'iosHomeIcon.png', rel: "android-touch-icon", size: "192x192", type: "image/png"%>
    <%= favicon_link_tag 'iosHomeIcon.png', rel: "ios-touch-icon", size: "180x180", type: "image/png"%>

  </head>
  <body>
    <script type="text/javascript">
      $(document).ready(function() {
        $('#is-open').hide();
        $('.navi').hide();
        $('#is-close').click(function(){
          $('#is-open').show();
          $('#is-close').hide();
          $('.navi').show();
        });
        $('#is-open').click(function(){
          $('#is-open').hide();
          $('#is-close').show();
          $('.navi').hide();
        });
      });
  </script>
    <header>
      <div class="hamburger-menu" id="is-open">
        <%= image_tag 'hamburger-menu-cross.png'%>
      </div>
      <div class="hamburger-menu" id="is-close">
        <%= image_tag 'hamburger-menu-icon.png'%>
      </div>
    </header>
    <div class="navi">
      <ul>
        <p>メニュー</p>
        <li><%= link_to 'トップページ', '/' %></li>
        <li><%= link_to 'カラオケを検索', '/search' %></li>
        <li><%= link_to 'お知らせ', '/information' %></li>
        <!--<li><%= link_to 'ご意見箱', '/contact' %></li>-->
      </ul>
    </div>
    <%= yield %>
  </body>
</html>
